name: Reveil Automatique
on:
  schedule:
    - cron: '*/5 * * * *'  # "Toutes les 5 minutes"
  workflow_dispatch:      # Permet de le lancer à la main pour tester

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Appeler l'URL de test
        run: curl https://ErwanBRT.pythonanywhere.com/run-my-tests-secret-99